export { SP_TOKENS } from './tokens';
export type { SuperProfesTokens } from './tokens';

/** Path where the DS stylesheet is served in the deployed apps. */
export const DESIGN_CSS_PATH = '/design-system/colors_and_type.css';
