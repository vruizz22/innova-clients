Expo Mobile app scaffold

To run locally:

```
cd innova-clients/apps/mobile
pnpm install
pnpm start
```

CI/Deploy: use EAS Build (set `EAS_TOKEN` as GitHub secret) and follow the workflow in `.github/workflows/deploy-expo.yml` at repo root.
